import React from "react";

const Header = () => {
  return (
    <h2
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        background: "#9448BC",
        color: "white",
        padding: "20px",
        marginBottom: "50px",
      }}
    >
      Generate, Download & Scan QR
    </h2>
  );
};

export default Header;
